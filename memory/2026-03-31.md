# Memory — 2026-03-31

## Workspace Access Issues Resolved

- **TCC (Transparency, Consent, and Control)** — macOS Full Disk Access needed for shell access to `/Volumes/Cowboy_One`. Grant via System Settings → Privacy & Security → Full Disk Access → add Terminal.app. This resolved the "Operation not permitted" errors when accessing mounted volumes from shell.
- The workspace is at `/Users/redlexGilgamesh/Ws/Openclaw/openclaw-librarian-agent/`
- Data is at `/Volumes/Cowboy_One/agent/openclaw-librarian-agent/`
- Vault is Google Drive mount at `~/Library/CloudStorage/.../AI/LibrarianVault/`

## Vault Location Issues

- **News briefings** from journalist_agent were being filed to `RIB/reports/news/` — André confirmed these are **Personal**, not RIB. Always file news/weather briefings to `Personal/reports/news/` and `Personal/reports/weather/`.
- Same applies to weather briefings — moved from `RIB/reports/weather/` to `Personal/reports/weather/`.

## Processed Folder vs Staging

- **Workspace `processed/` folder** (`/Users/redlexGilgamesh/Ws/Openclaw/openclaw-librarian-agent/processed/`) is the correct place to look for files the Elixir pipeline processed but that weren't filed to vault. This is separate from `Cowboy_One`'s `processed/`.
- **Elixir `mark_filed/2` bug**: Sets `status: filed` without enforcing `vault_path`. This creates orphans — items marked filed in DB but never written to vault. Caused by: (1) IAMQ/Mix exec getting stuck due to MqWsClient crashing, (2) agent-side filing failing before `mark_filed` is called. Symptom: items pile up in staging with `status: pending`.
- **6 orphans from Mar 21-25** were manually filed today. All are now in vault.

## Inbound Folder

- OpenClaw inbound media is at `/Users/redlexGilgamesh/.openclaw/media/inbound/`
- Contains: PDF receipts, images, voice memos (`.ogg`), CSV data exports, OAuth JSON credentials
- OAuth JSON files (`file_0.json` type) should NOT be filed to vault — sensitive credentials
- Zepp Aura sleep CSVs and FritzBox data should go to `Personal/health/` or `Personal/security/`

## MqWsClient Crash Loop

- `MqWsClient` (WebSocket-based MQTT client) is crashing every ~15 seconds with `connection refused` on port 18790. It's in the OTP supervisor tree but is `one_for_one` so doesn't kill other services.
- It causes `mix run` commands to hang when run via `docker exec` because the crash/reconnect loop floods stdout.
- Fix: either disable MqWsClient in `application.ex` or ensure the MQTT broker it expects is actually running.

## IAMQ Status

- IAMQ HTTP polling IS working (checked Mar 31) — messages are being delivered via HTTP to port 18789
- Old messages in `log/iamq-*.json` (Mar 21-23) are **log files**, not queue files — those were created during downtime as message records, not actionable items
- The actual queue is at `~/.openclaw/openclaw-inter-agent-message-queue/queue/librarian_agent/`

## Vault Audit Findings (Mar 26-31)

- **March 27**: No documents exported/produced — André didn't work that day. Pipeline is fine.
- **March 28-30**: Also no exports. Pipeline silent.
- **March 30-31**: journalist_agent produced news briefings filed to `RIB/reports/news/` (wrong location) — moved to `Personal/reports/news/`
- Many meetings from Mar 27 and Mar 30 were found in workspace `processed/` (not Cowboy_One) — all manually filed to vault today
- Key meetings recovered: AI-Steerco, AI-setup-deep-dive, Dry-Run-AI-All-Hands, R&D-Goal-Setting-Follow-Up, Track-1/2 Plan Alignment, Weekly 4.0 x Hub Check-In

## Deep-Amber Project

- Files filed under `Entrepreneur/deep-amber/` (confirmed correct location per André)
- Glossary multilingual filed there, not in `RIB/reference/`
- Two glossary files existed — duplicate in `RIB/reference/` was removed
